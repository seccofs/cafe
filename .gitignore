/target
**/target
*.pdb

# Real-content corpus categories (see AGENTS.md phase 2 and corpus/
# ATTRIBUTION.md) are now populated with curated, license-checked source
# images and ARE tracked, same as the synthetic categories generated by
# `cafe-bench --bin gen-corpus`. `corpus/hdr/` holds curated `.exr`
# fixtures, registered into manifest.json (see AGENTS.md's HDR benchmark
# wiring and corpus-population follow-ups), and is tracked too, since
# it's license-checked the same way as every other category.

# Fuzzing artifacts/corpus (regenerable, can be large)
/fuzz/artifacts
/fuzz/corpus

# Frozen v1.12.0 reference implementation (see AGENTS.md) and the author's
# personal design notes are kept on disk for local reference but are
# intentionally excluded from version control for this repository.
/old/
/cafe.docx
